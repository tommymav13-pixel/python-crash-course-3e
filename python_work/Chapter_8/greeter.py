def greet_user(username):                   # parantheses hold the information
        """Display a simple greeting."""    # docstring, describes what the
                                            # function does
        print(f"Hello, {username.title()}!")# Line of actual code

greet_user('jesse')                         # function call